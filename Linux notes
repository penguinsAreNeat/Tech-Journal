Users:
  adduser [user]
  passwd [user]
  usermod -aG sudo [user] - replace sudo with wheel for CentOS7

Networking
  hostnamectl set-hostname
  nmtui - terminal IP GUI and hostname setter

Firewall
  ifconfig
  firewall-cmd --list-all
  firewall-cmd --zone=public --permanent --add-port=[port]/(tcp/udp)
