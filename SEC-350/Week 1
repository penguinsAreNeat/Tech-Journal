passwd
adduser
usermod -aG
hostnamectl set-hostname
set Linux ip with nmtui

vyos config:
  configure
  set sytem host-name [hostname]
  commit
  save
  exit

firewall-cmd --list-all

Linux IP routes are finicky, recommend using CLI instead of GUI due to inconsistent updating
